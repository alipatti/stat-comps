\section{Introduction}

\begin{frame}{Statistics from 10,000 ft}

	You have some outcome $y$ that you assume is related to a variable of interest $y$ by some process $f$:
	\begin{equation}
		y = f(x) + \epsilon_i.
	\end{equation}

	\pause \bigskip

	\textbf{Examples}

	\begin{itemize}
		\item ${x_i}$ are characteristics of a house, ${y_i} \in \R$ is its market price
		\item ${x_i}$ are medical scans, ${y_i} \in \set{0, 1}$ is "does this patient have cancer"
	\end{itemize}

	\pause \bigskip

	\textbf{Goal:} Estimate $f$ given some sets ${x_i}$ and ${y_i}$.

	\begin{itemize}
		\item Inference: how are $x$ and $y$ related?
		\item Prediction: given $x$, what's my best guess for $y$?
	\end{itemize}
\end{frame}

\begin{frame}{Interpretability v. Flexibility}

	% TODO: put something better on top here

	\textbf{Linear regression:}
	\begin{itemize}
		\item $f(x) = Ax + b$
		\item Can get fancy and transform predictors
	\end{itemize}

	\pause \bigskip

	\textbf{Logistic regression:}
	\begin{itemize}
		\item $f(x) = \sigma(Ax + b)$
	\end{itemize}

	\paus \bigskip

	\begin{center}
		\tikz[every node/.style={align=center, font=\small}]{
			\draw[<->]
			(-4,0) node[below, font=\itshape \tiny]
			{Interpretable \\ (better for interance)}
			-- (4,0) node[below, font=\itshape \tiny]
			{Flexible \\ (better for prediction)};

			\node<+-> at (-3, .7) {Linear \\ regression};
			\node<+-> at (.5, 1.4) {Polynomial \\ regression};
			\node<+-> at (-2, 1.5) {GLM};
			\node<+-> at (2, 1) {Splines};

			\node<+-> at (-1, .9) {Nearest \\ neighbors};
			\node<+-> at (3.1, 1.7) {Local \\ regression};

			\node<+-> at (1.8, 2) {SVMs};
			\node<+> at (4, .7) {Neural \\ networks};
			\node<+> [font=\bfseries, red] at (4, .7) {Neural \\ networks};
		}
	\end{center}
\end{frame}
